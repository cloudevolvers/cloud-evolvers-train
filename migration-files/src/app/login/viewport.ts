import type { Viewport } from 'next';

// Add any specific viewport settings needed for the login page here
// If none are needed, an empty object or default settings are fine.
export const viewport: Viewport = {
  // Example: themeColor: 'black',
};
